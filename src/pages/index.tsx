import { Box } from "@chakra-ui/react";
import Footer from "@/components/footer";
import Hero from "@/components/hero";

const Home = () => {
  return (
    <Box w={"100%"}>
      <Hero />
      <Footer />
    </Box>
  );
};

export default Home;
